<html>
    <head>
    <meta charset="UTF-8">
    <link href="/styles/style.css" rel="stylesheet">
    </head>
    <body>
        <header>

            <h1>Flashcard</h1>
            <h3> As-tu bien revise ?</h3>
        </header>

        <div class="subject">
            <div class="cardsColumn">
                {% for card in cardsList %}
                    <label class="card cardQuestion">{{card.questionRecto}} </label>
                {% endfor %}
            </div>
            <div class="cardsColumn">
                {% for card in cardsList %}
                    <label class="card cardResponse is-hidden">{{card.responseVerso}} </label>
                {% endfor %}
            </div>
            <div class="cardsColumn">
                {% for card in cardsList %}
                    <div class="card checkboxField">
                        <input type="checkbox" />
                    </div>
                {% endfor %}
            </div>
        </div>
        

        <footer>
            © Margot Rasamy - 2020
        </footer>
        <script type="text/javascript" src="/scripts/script.js"></script>
         <script type="text/javascript" src="/scripts/score.js"></script>
    </body>
</html>