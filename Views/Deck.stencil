<html>
    <head>
    <meta charset="UTF-8">
    <link href="/styles/style.css" rel="stylesheet">
    </head>
    <body>
        <header>

            <h1>Flashcard</h1>
            <h3> As-tu bien revise ?</h3>
        </header>

        <div class="subject">
                <div class="cardsColumn">
                    {% for eachDeck in allDecks %}
                            {% for cards in eachDeck.deck %}
                                <div class="card cardQuestion">{{cards.questionRecto}} </div>
                                
                            {% endfor %}
                    {% endfor %}
                </div>

                <div class="cardsColumn">
                    {% for eachDeck in allDecks %}
                            {% for cards in eachDeck.deck %}
                            
                                <div class="card cardResponse is-hidden">{{cards.responseVerso}} </div>
                            {% endfor %}
                    {% endfor %}
                </div>
        </div>
        

        <footer>
        © Margot Rasamy - 2020
        </footer>
        <script type="text/javascript" src="/scripts/script.js"></script>
    </body>
</html>